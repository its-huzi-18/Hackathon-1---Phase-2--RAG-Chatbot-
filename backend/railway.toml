# Railway configuration for the backend
[build]
builder = "NIXPACKS"
workdir = "."
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "uvicorn app:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
numReplicas = 1
region = "us-west"

